tail -f log.txt | grep 'keyword' &
echo $!

(ls | cat) & echo $!

echo "Start" && (sleep 1; echo "End")

(echo nested && (echo deeper | cat)) || echo "Failed"

echo 'Single quotes with $NOT_EXPANDED'

export VAR="Hello" && echo $VAR

echo '$VAR'

ls |

echo "Backslash test \\"

CtrlC when secondary prompt
